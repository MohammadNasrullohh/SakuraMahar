const reportWebVitals = (onPerfEntry) => {
  if (onPerfEntry && typeof onPerfEntry === 'function') {
    // No-op performance reporting for this app build.
    // If you want metrics later, install 'web-vitals' and update this function.
  }
};

export default reportWebVitals;

